# Logs

Raw text files, useful for debugging or sharing output between machines (like Colab and laptop) without needing the overhead of formats like CSV or worrying about file conversion.

- `04-05-26.txt` : All simulations except neuromorphic running with equivalent statistics for N=1000 and p=0.5